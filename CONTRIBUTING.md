# 协作规范（CONTRIBUTING）

> 5 人小组协作开发约定。动手写代码前先读完本文；本文与《图书管理系统-接口契约》不一致时，以契约为准。

## 1. 分支模型

| 分支 | 用途 | 说明 |
|---|---|---|
| `main` | 稳定版本 | **受保护**：只能通过 PR 合入，禁止直接 push / 强制推送 / 删除，合并前需 1 人审批 |
| `dev` | 每周集成分支 | 所有人可 push，周五集成日统一合并 |
| `feature/<模块>` | 个人开发分支 | 每人一个，按板块命名 |

分支命名示例：`feature/base`（基础层+登录）、`feature/reader`（读者管理）、`feature/book`（图书管理）、`feature/borrow`（借阅管理）、`feature/statistic`（统计+系统管理）。

## 2. 日常流程

1. 动手前先同步最新代码：`git checkout dev && git pull`；
2. 从最新的 dev 切出自己的分支：`git checkout -b feature/xxx`；
3. 开发并提交，**每天至少 push 一次**到自己的 feature 分支（防止本地丢失）；
4. 每周五集成日：feature 分支向 `dev` 发起 Pull Request，组长主持合并、解决冲突；
5. 里程碑联调完成后：`dev` → `main` 发起 PR，1 人审批通过后合并。

## 3. 提交信息规范

格式：`<类型>: <中文描述>`，一个提交只做一件事。

| 类型 | 用途 | 示例 |
|---|---|---|
| feat | 新功能 | `feat: 读者管理面板增删改查` |
| fix | 修 BUG | `fix: 库存不足时仍生成借阅记录的问题` |
| docs | 文档 | `docs: 补充数据库说明` |
| refactor | 重构（不改功能） | `refactor: 抽取公共表格刷新方法` |
| test | 测试 | `test: 还书罚款计算用例` |
| chore | 构建/配置/杂项 | `chore: 更新 gitignore` |

不要向 dev / main 提交无法编译的代码。

## 4. 接口契约规则（最重要）

1. 实体字段、DAO 接口签名、数据库表结构已在契约中**冻结，不许私自修改**；
2. 确需变更：在群里说明影响 → 全组同意 → 更新契约文档 → 通知所有人；
3. 跨模块取数据**只能调用对方 DAO 接口**，禁止直接写他人负责表的 SQL；
4. 每个文件有唯一主人（见契约"文件归属总表"），不改别人的文件；公共问题在群里说。

## 5. 代码规范

- Java SE（JDK 8+），**不引入 SSH / SpringBoot 等第三方框架**（课程硬性要求）；
- 所有 SQL 一律用 `PreparedStatement`，禁止字符串拼接 SQL；
- 实体类必须 `implements Serializable`，含无参构造 + 全字段 getter/setter；
- 库存增减只能调 `BookDao.updateStock(bookId, delta)`，借阅模块不直接改 book 表；
- 界面统一：窗口 1024×728，表格用 `BaseTableModel<T>` + JTable，风格参考教材第九章示例；
- 本地数据库统一执行 `sql/library.sql` 初始化，禁止私自改表结构。

## 6. 冲突处理

- 从源头减少冲突：一个文件只有一个主人，公共契约文件尽量少动；
- 契约文件（实体 / DAO 接口）冲突由组长统一处理；
- 解决不了的冲突在群里拉人一起看，不要强行覆盖他人代码。

## 7. 本地环境

- JDK 8+；MySQL 5.7 / 8.0，库名 `library`，字符集 utf8；
- 数据库账号密码在 `JDBCConnection` 中，各自本地按需修改；**不要把改了连接配置的文件随手推上来**，提交前检查 diff。
